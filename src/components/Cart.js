import { useEffect } from "react";
import { useSelector } from "react-redux";

const Cart = () => {
  useEffect(() => {
    window.scrollTo(0, 0);
    // console.log(cartItems);
  }, []);

  // const cartItems = useSelector(store => store);

  return (
    <>
      <div>hellohkkjhkh</div>
    </>
  );
};

export default Cart;
